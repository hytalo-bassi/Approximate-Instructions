#include "../instructions.h"
#include <stdio.h>

int main() {
    int a = 10, b = 20, res = 0;
    
    // --- Initialization Code ---
    // (Prepare arrays, images, etc.)

    res = a+b;

    return 0;
}